app:
  config:
    namespace: ${NAMESPACE:accelbyte}
    resource_name: 'MMV2GRPCSERVICE'
  security:
    client_id: '123'
    client_secret: 'abc'

justice:
  grpc:
    interceptor:
      auth:
        enabled: true
      debug-logger:
        enabled: false